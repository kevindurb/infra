---
default_config:
assist_pipeline:
http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 0.0.0.0/0

conversation:
  intents:
    forecast:
      - 'Whats the forecast?'

intent_script:
  forecast:
    speech:
      text: 'It is currently {{ states("weather.forecast_home") }}'

automation: !include automations.yaml
