---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    gethomepage.dev/href: 'https://music-assistant.durbin.casa'
    gethomepage.dev/enabled: 'true'
    gethomepage.dev/app: music-assistant
    gethomepage.dev/group: Apps
    gethomepage.dev/icon: mdi-speaker
    gethomepage.dev/name: Music Assistant
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: ingress-nginx/htpasswd
    nginx.ingress.kubernetes.io/auth-realm: durbin.casa
spec:
  ingressClassName: nginx
  rules:
    - host: music-assistant.durbin.casa
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: service
                port:
                  name: web
