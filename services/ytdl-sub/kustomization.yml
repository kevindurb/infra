---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: ytdl-sub-

resources:
  - ./resources/deployment.yml

configMapGenerator:
  - name: config
    files:
      - ./resources/subscriptions.yml

commonLabels:
  app.kubernetes.io/name: ytdl-sub
