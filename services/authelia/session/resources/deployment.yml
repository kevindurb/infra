---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      containers:
        - name: session
          image: docker.io/apache/kvrocks
          resources:
            limits:
              memory: 1G
          ports:
            - name: session
              containerPort: 6666

---
apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  ports:
    - name: session
      port: 6379
      targetPort: session
