---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: calibre-desktop-

resources:
  - ./resources/calibre-desktop.yml

labels:
  - pairs:
      app.kubernetes.io/name: calibre-desktop
    includeSelectors: true
    includeTemplates: true
