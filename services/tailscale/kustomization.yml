---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./resources/tailscale.yml

secretGenerator:
  - name: tailscale
    envs:
      - tailscale.env

labels:
  - pairs:
      app.kubernetes.io/name: tailscale
    includeSelectors: true
    includeTemplates: true
