version: "3.9"
services:
  # This is cool and all but docker sock access...
  # proxy:
  #   image: nginx-proxy/nginx-proxy
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - /var/run/docker.sock:/tmp/docker.sock:ro
  whoami:
    image: jwilder/whoami
    environment:
      - VIRTUAL_HOST=whoami.*
  homeassistant:
    image: homeassistant/home-assistant
    environment:
      - VIRTUAL_HOST=hass.*
      - VIRTUAL_PORT=8123
  grafana:
    image: grafana/grafana
    environment:
      - VIRTUAL_HOST=grafana.*
      - VIRTUAL_PORT=3000
