# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3'

includes:
  templates: .taskfiles/templates
  authelia: .taskfiles/authelia
  ansible: .taskfiles/ansible
  argo: .taskfiles/argo
  cloudflare: .taskfiles/cloudflare

tasks:
  setup:
    cmds:
      - task: ansible:setup
      - npm install

  lint: npx prettier -w .
