---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-config
  labels:
    gatus.io/enabled: 'true'
data:
  calibre.yml: |
    endpoints:
      - name: calibre
        enabled: false
        url: http://calibre-service.default
        conditions:
          - '[STATUS] == 200'
