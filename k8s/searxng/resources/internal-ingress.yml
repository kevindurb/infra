---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer

    gethomepage.dev/href: 'https://searxng.durbin.casa'
    gethomepage.dev/enabled: 'true'
    gethomepage.dev/app: searxng
    gethomepage.dev/group: Other
    gethomepage.dev/icon: searxng
    gethomepage.dev/name: Searxng
    gethomepage.dev/widget.type: searxng
    gethomepage.dev/widget.url: http://searxng-service

spec:
  ingressClassName: internal
  rules:
    - host: &host searxng.durbin.casa
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: service
                port:
                  name: http
  tls:
    - secretName: durbin-casa-wildcard-cert
      hosts:
        - *host
