---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: 'HTTPS'
    gethomepage.dev/href: 'https://truenas.durbin.casa'
    gethomepage.dev/enabled: 'true'
    gethomepage.dev/app: truenas
    gethomepage.dev/group: Infra
    gethomepage.dev/icon: truenas
    gethomepage.dev/name: TrueNAS
    gethomepage.dev/widget.type: truenas
    gethomepage.dev/widget.url: http://truenas-service
    gethomepage.dev/siteMonitor: http://truenas-service
    gethomepage.dev/external: 'true'
spec:
  ingressClassName: nginx
  rules:
    - host: truenas.durbin.casa
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: service
                port:
                  name: https
