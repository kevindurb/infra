---
apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  selector:
    app.kubernetes.io/component: web
  ports:
    - name: web
      port: 3000
      targetPort: web
    - name: streaming
      port: 4000
      targetPort: streaming
