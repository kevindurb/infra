---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer

    gethomepage.dev/href: 'https://mastodon.durbin.casa'
    gethomepage.dev/enabled: 'true'
    gethomepage.dev/app: mastodon
    gethomepage.dev/group: Other
    gethomepage.dev/icon: mastodon
    gethomepage.dev/name: Mastodon
    gethomepage.dev/widget.type: mastodon
    gethomepage.dev/widget.url: http://mastodon-service

spec:
  ingressClassName: internal
  rules:
    - host: &host mastodon.durbin.casa
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: service
                port:
                  name: http
  tls:
    - secretName: durbin-casa-wildcard-cert
      hosts:
        - *host
