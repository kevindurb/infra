---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer

    gethomepage.dev/href: 'https://rss.durbin.casa'
    gethomepage.dev/enabled: 'true'
    gethomepage.dev/app: rss
    gethomepage.dev/group: Productivity
    gethomepage.dev/icon: rss
    gethomepage.dev/name: RSS

spec:
  ingressClassName: internal
  rules:
    - host: &host rss.durbin.casa
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: service
                port:
                  name: http
  tls:
    - secretName: durbin-casa-wildcard-cert
      hosts:
        - *host
