---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels: &labels
    app.kubernetes.io/component: web
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      volumes:
        - name: volume
          persistentVolumeClaim:
            claimName: volume
      hostNetwork: true
      nodeSelector:
        nvidia.com/gpu.present: 'true'
      runtimeClassName: nvidia
      containers:
        - name: scrypted
          image: ghcr.io/koush/scrypted:v0.119.2-jammy-nvidia@sha256:aa89d00b4cd3430187cd937c7db58f8cf2cbea99d0aa94b2f7f5d987474ae7c1
          env:
            - name: SCRYPTED_DOCKER_AVAHI
              value: 'true'
          resources:
            requests:
              memory: 750M
            limits:
              nvidia.com/gpu: 1
              memory: 1G
          volumeMounts:
            - name: volume
              mountPath: /server/volume
          ports:
            - name: web
              containerPort: 11080
