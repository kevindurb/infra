---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels: &labels
    app.kubernetes.io/component: web
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      volumes:
        - name: volume
          persistentVolumeClaim:
            claimName: volume
      hostNetwork: true
      nodeSelector:
        nvidia.com/gpu.present: 'true'
      runtimeClassName: nvidia
      containers:
        - name: scrypted
          image: ghcr.io/koush/scrypted:v0.123.47-jammy-nvidia@sha256:876701c903ae4913cd1b29adf837b4a69274491919b1218a2b329101d074e827
          env:
            - name: SCRYPTED_DOCKER_AVAHI
              value: 'true'
          resources:
            requests:
              memory: 750M
            limits:
              nvidia.com/gpu: 1
              memory: 1G
          volumeMounts:
            - name: volume
              mountPath: /server/volume
          ports:
            - name: web
              containerPort: 11080
