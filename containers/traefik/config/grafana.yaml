---
http:
  routers:
    grafana:
      rule: "HostRegexp(`grafana.{domain}`) || HostRegexp(`grafana.{domain}.{tld}`)"
      service: grafana
  services:
    grafana:
      loadBalancer:
        servers:
          - url: 'http://grafana:3000'
