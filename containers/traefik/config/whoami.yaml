---
http:
  routers:
    whoami:
      rule: "HostRegexp(`whoami.{domain}`) || HostRegexp(`whoami.{domain}.{tld}`)"
      service: whoami
      tls: {}
      middlewares:
        - basic-auth
  services:
    whoami:
      loadBalancer:
        servers:
          - url: 'http://whoami:8000'
