---
http:
  routers:
    freshrss:
      rule: "HostRegexp(`freshrss.{domain}`) || HostRegexp(`freshrss.{domain}.{tld}`)"
      service: freshrss
      middlewares:
        - basic-auth
      tls: {}
  services:
    freshrss:
      loadBalancer:
        servers:
          - url: 'http://freshrss'
