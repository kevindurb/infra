---
http:
  routers:
    hass:
      rule: "HostRegexp(`hass.{domain}`) || HostRegexp(`hass.{domain}.{tld}`)"
      service: hass
      tls: {}
      middlewares:
        - basic-auth
  services:
    hass:
      loadBalancer:
        servers:
          - url: 'http://homeassistant:8123'
