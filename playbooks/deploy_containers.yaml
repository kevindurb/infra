# vim: set ft=yaml.ansible:
---
- hosts: all
  remote_user: kevindurb
  tasks:
    - copy:
        src: "./docker-compose.yaml"
        dest: "~/docker-compose.yaml"

    - docker_compose:
        restarted: yes
        project_src: "~"
